# AI 弹幕助手 - 使用说明

## 快速开始

### 方式一：双击运行（推荐）

```
release/DanmuAssistant.exe
```

直接启动，无需任何配置。

### 方式二：命令行运行

```bash
cd W:\Games\Hanako\Work\danmu-assistant
python main.py
```

### 方式三：命令行参数

```bash
python main.py --no-audio       # 禁用语音输入
python main.py --no-screen      # 禁用截屏
python main.py --config xxx.yaml # 指定配置文件
```

## 功能说明

### 1. 弹幕浮层（透明窗口）

- 显示在屏幕顶部，半透明
- 弹幕从右向左滚动
- **可以拖拽移动** — 鼠标按住拖动
- 10 条轨道均匀分布
- 弹幕到达左侧后淡出消失

### 2. 统计面板（灰色小窗口）

- 显示弹幕总数、情绪分布、高频词
- **可以拖拽移动** — 鼠标按住拖动
- 每秒自动刷新

### 3. 截屏流水线（自动运行）

- 每 8 秒自动截屏一次
- 画面变化时生成弹幕
- 通过 Agnes AI 分析画面内容

### 4. 语音流水线（需要麦克风）

- 每 15 秒采集一次麦克风
- 使用 FunASR 本地语音转写
- 识别到语音后生成弹幕

## 配置

### 网页配置工具（推荐）

```
双击 release/config_web.html
```

- 调整 API、截屏、语音、Hanako 等所有参数
- 点击「保存配置」自动下载 config.yaml
- 替换 release/ 目录下的 config.yaml

### 手动编辑 config.yaml

```yaml
capture:
  enabled: true        # 是否启用截屏
  interval: 8          # 截屏间隔（秒）
  quality: 70          # 图片压缩质量

audio:
  enabled: true        # 是否启用语音
  interval: 15         # 语音采集间隔（秒）

hanako:
  enabled: false       # Hanako 联动（默认关闭）
```

## 常见问题

### Q: 没有弹幕出现？

A: 检查以下几点：
1. 截屏是否启用 — `capture.enabled: true`
2. 画面是否有变化 — 静止画面不会触发
3. API 是否正常 — 检查 config.yaml 中的 api_key
4. 看控制台输出 — 有没有报错信息

### Q: 语音没反应？

A: 首次运行 FunASR 会下载模型（约 200MB），请耐心等待。
确认麦克风权限已授予 Python。

### Q: 弹幕太小/太大？

A: 在 config_web.html 中调整「字体大小」滑块，或编辑 config.yaml：
```yaml
danmu:
  font_size: 16  # 改这个数字
```

### Q: 怎么关闭某个功能？

A: 在 config.yaml 中设置：
```yaml
capture:
  enabled: false   # 关闭截屏
audio:
  enabled: false   # 关闭语音
```

### Q: 打包的 exe 太大了？

A: PyInstaller 打包了 PyQt5 等依赖，86MB 是正常的。
如果需要更小体积，可以考虑 cx_Freeze 或 nuitka。

## 文件说明

```
danmu-assistant/
├── main.py              # 主入口
├── stream_engine.py     # 流式引擎（截屏+语音）
├── audio_input.py       # 语音输入（FunASR）
├── screen_capture.py    # 截屏模块
├── inference.py         # LLM 弹幕生成
├── danmu_widget.py      # 弹幕浮层
├── danmu_stats_panel.py # 统计面板
├── config.yaml          # 配置文件
├── config_web.html      # 网页配置工具
├── release/             # 发布目录
│   ├── DanmuAssistant.exe
│   ├── config.yaml
│   └── config_web.html
└── README.md            # 本文件
```

## 技术支持

- 查看控制台输出获取详细日志
- 配置文件默认路径：`config.yaml`
- 历史记录保存：`danmu_history.json`
